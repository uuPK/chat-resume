global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: chat-resume-backend
    metrics_path: /metrics
    static_configs:
      - targets:
          - host.docker.internal:8000
        labels:
          app: chat-resume
          service: backend
